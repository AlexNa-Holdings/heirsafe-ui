// src/version.ts
/** App version in the format 1.x (minor auto-bumps on `npm run deploy`) */
export const APP_VERSION = "1.2";
export const APP_GIT_TAG = `v${APP_VERSION}`;
export default APP_VERSION;
